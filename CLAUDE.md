# mitsubachi-token

Mitsubachi デザインシステムのデザイントークン。Figma から取得して css / scss / TypeScript に変換し、npm パッケージ `@uzabase/mitsubachi-token` として配布する。

## ドキュメント（迷ったらここを正とする）

開発・リリースの判断基準はすべて `docs/` にある。手順や基準を推測で補わず、必ず該当ファイルを読むこと。

- `docs/contributing.md` … 生成フロー・ディレクトリ構造・セットアップ・開発コマンド・トークンの命名・CI の挙動
- `docs/versioning.md` … semver 方針・後方互換性チェック・削除とリネームの手順（廃止期間）

## 絶対に守ること

- **`lib/` 配下は生成物。手で編集しない。** Figma か `tools/` 側を直して再生成する。
- **色トークンの真実は Figma。** 唯一の例外は後方互換用の `tools/tokens/deprecated/base.json`。
- **バージョンは `npm run check:compat` の出力で決める。** 目視の差分で判断しない。

## やり取りの原則（重要）

- **指摘が先、修正は承認後**。問題点はまず一覧で示し、私の承認後に直す。
- **一度に全部直さない。1つずつ**提案し、私が次に進む合図を出すまで待つ。
- 私は JS / TS にあまり詳しくない。指摘には「なぜ問題か」「どう直すか」「優先度（高/中/低）」を添える。
- **トークンの削除・値変更を勝手に確定させない**。利用者を壊す変更なので、必ず一覧で示して承認を得る。
- **バージョン上げは PR レビューの後**。レビュワーが変更内容を確認する前に `package.json` を書き換えない。

## スキル

- Figma からのトークン更新〜PR → `/update-tokens`
