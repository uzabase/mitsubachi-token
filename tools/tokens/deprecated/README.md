# deprecated

後方互換性のために残しているトークンのエイリアスを置く。Figma から削除・リネームされたトークンの旧名をここに追加すると、既存の利用者が壊れず、リリースが major にならない。

**書き方と運用（廃止期間・`comment` の形式・次の major での削除）は [`docs/versioning.md`](../../../docs/versioning.md) の「削除・リネームの手順」を正とする。**

エントリが無いときは `base.json` は `{}` のままにする。
